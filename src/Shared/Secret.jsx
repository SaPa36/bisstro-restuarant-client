import React from 'react';

const Secret = () => {
    return (
        <div>
            <h2 className="text-2xl font-bold text-center">This is a secret page. Only logged in users can see this.</h2>
        </div>
    );
};

export default Secret;